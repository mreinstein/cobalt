{
  "name": "cobalt",
  "private": true,
  "type": "module",
  "main": "bundle.js",
  "scripts": {
    "build": "node esbuild.js",
    "prepublishOnly": "npm run build"
  },
  "devDependencies": {
    "esbuild": "^0.19.1",
    "esbuild-plugin-glsl": "^1.1.0"
  }
}
